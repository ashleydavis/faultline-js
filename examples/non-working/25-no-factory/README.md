# A parameter no value can be built for

A type whose value no signature decides. The run says which function takes it and asks for a test input factory, and that function's own paths are left off the list.
