// A branch no value made up from the type reaches.

export function isMagic(word: string): boolean {
    if (word === "the-one-word-no-run-makes-up") {
        return true;
    }
    return false;
}
