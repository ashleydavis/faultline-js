# Code that runs in a browser

A function that reads the document has nowhere to run in Node. With `--browser` the run loads the copies into a real page, calls them there, and reads what V8 counted in the browser, which is the same thing V8 reports in Node.

The branch on an empty title is one no made up call reaches, because a page has a title. The scenario beside it sets one and calls again.
