# More than one directory of source

A project whose code sits in two trees. Name each with `--source` and the walk takes those and nothing else, so a directory of fixtures beside them is left where it is.
