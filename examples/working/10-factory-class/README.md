# A factory that hands back a different value each time

A factory written as a method on an exported class. The run builds the class once and calls the method for every value it needs, so one factory walks a sequence of its own.
