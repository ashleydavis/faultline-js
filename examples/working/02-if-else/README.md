# An if and its else

Both sides of an `if`, and an `if` with no `else` at all. Neither needs anything written for it: the false side is counted as the times the condition was reached and the true side was not.
