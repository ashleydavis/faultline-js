# An ordinary Node project

A `package.json`, a `tsconfig.json` and a `src` directory, which is how a Node project written in TypeScript is laid out. Faultline reads the `tsconfig.json`, so it sees the same types the editor does, and walks `src` without being told to.
