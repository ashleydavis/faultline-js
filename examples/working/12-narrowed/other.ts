// Left out of the run, because it was narrowed to the file beside this one.

export function neverMeasuredHere(text: string): number {
    if (text === "the one value nothing makes up") {
        return 1;
    }
    return 0;
}
