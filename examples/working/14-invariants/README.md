# An invariant

Something that has to hold after every call a run makes, and after every scenario.

An invariant is found by its one parameter, which is what tells it from a scenario. It takes the subject and nothing else.
