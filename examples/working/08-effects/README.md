# Taking the clock, the network, the files and the writer as parameters

A function that takes one of these is handed the run's own, which answers and fails in turn, so the paths that only run when something goes wrong are reached without a scenario.

The one path left is the settings parsing and saying nothing about retries, which no made up call reaches, and the sim file beside it holds the scenario that does.
