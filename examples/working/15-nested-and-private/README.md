# A function the file does not export

A private function gets a line of its own, and its paths are ticked by whatever calls it. A callback written inside another function gets no line: its paths belong to the function that wrote it, which is the function somebody has to go and change.
