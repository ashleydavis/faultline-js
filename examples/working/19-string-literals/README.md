# A branch that turns on one particular string

A run makes up strings from a list of the ones worth trying, so it will not land on a word your code is looking for. A scenario is what reaches each one.
