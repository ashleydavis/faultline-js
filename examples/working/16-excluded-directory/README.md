# A directory left out of the walk

Your benchmarks and your fixtures are code, so the walk takes them for source and asks you to cover their paths too. Name them with `--exclude` and they are left out, with the reason said in the report.
